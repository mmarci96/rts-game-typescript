server:
  host: "0.0.0.0"
  listen_port: "8000"

static:
  dir: "/home/meirl/Documents/film-media-hub/client/dist"

connection:
  - name: Server 
    endpoint: /ws/
    destination_url: "http://localhost:8080/"
  - name: API
    endpoint: /api/
    destination_url: "http://localhost:5000/"
