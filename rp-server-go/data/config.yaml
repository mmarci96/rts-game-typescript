server:
  host: "0.0.0.0"
  port: "9000"

static:
  - name: UI
    endpoint: "/"
    destination_url: "/"
    dist: 

bridge:
  - name: WsBridge
    endpoint: "^~ /socket/"
    destination_url: "http://localhost:3001/"

connections:
  - name: Server0 
    endpoint: /ws/world0/
    destination_url: "http://localhost:8080/"
  - name: Server1
    endpoint: /ws/world1/
    destination_url: "http://localhost:8081/"
  - name: Server2
    endpoint: /ws/world2
    destination_url: "http://localhost:8082/"
