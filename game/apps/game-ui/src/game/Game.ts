import { GameMap } from "@packages/game-data";
import AssetManager from "./data/AssetManager";

class Game {
    static WIDTH = window.innerWidth;
    static HEIGHT = window.innerHeight;

    constructor(map: GameMap, assets: AssetManager) {

    }
}

export default Game;
