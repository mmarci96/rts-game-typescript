{
  "name": "game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:ui": "pnpm --filter game-ui dev",
    "start:server": "pnpm --filter game-server dev",
    "start": "pnpm run start:ui & pnpm run start:server",
    "build": "pnpm recursive run build",
    "test": "pnpm recursive run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.4.1",
  "dependencies": {
    "@packages/game-data": "link:packages/game-data",
    "@packages/game-db": "link:packages/game-db",
    "game-db": "link:game-db"
  },
  "pnpm": {
    "ignoredBuiltDependencies": [
      "@swc/core",
      "esbuild"
    ],
    "onlyBuiltDependencies": [
      "@swc/core",
      "esbuild"
    ]
  }
}
